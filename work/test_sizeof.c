#include "includes/cub3d.h"
#include <stdio.h>

int main(void) 
{ 
    printf("sizeof(t_player) = %zu bytes\n", sizeof(t_player)); 
    printf("sizeof(t_cub) = %zu bytes\n", sizeof(t_cub)); 
    return 0; 
}
